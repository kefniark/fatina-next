{
  "name": "fatina-workspace",
  "version": "1.0.0",
  "description": "Fatina",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "lib:build": "turbo run lib:build",
    "test": "turbo test" 
  },
  "workspaces": {
    "packages": [
      "packages/*",
      "examples/*",
      "docs"
    ]
  },
  "devDependencies": {
    "turbo": "^1.8.1"
  }
}
